{
  "name": "hair-formula",
  "private": true,
  "version": "0.0.0",
  "description": "Hair dye color prediction — spectral mixing engine + minimal web UI",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @hair/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
